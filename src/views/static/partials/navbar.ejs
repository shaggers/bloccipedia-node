<nav class="navbar fixed-top navbar-light">
    <div class="container">
        <a id="navTitle" href="/" class="navbar-brand mb-0 h1">Bloccipedia</a>

        <span id="navRightItems" class="ml-auto">
                <a href="/wikis" class="text-dark">Wiki's</a>
            <% if(currentUser){ %>
                <a href="/users/sign_out" class="text-dark">Sign Out</a> 
            <% } else { %>
                <a href="/users/sign_up" class="text-dark">Sign Up</a>
                <a href="/users/sign_in" class="text-dark">Sign In</a>
            <% } %>
            <% if(currentUser){ %>
                <% if(currentUser.isStandard()){ %>
                    <a href="/upgrade" class="text-dark">Upgrade</a>
                <% } else if (currentUser.isPremium()) { %>    
                    <a href="/downgrade" class="text-dark">Downgrade</a>
                <% } %>
            <% } %>
        </span>
    </div>
</nav>
